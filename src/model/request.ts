import {Careers} from "./subject";
import {RequestDTO} from "../services/requestDTO";

class SubjectsRequest {

    constructor(availableSubjects: Careers, requestDTO: RequestDTO) {
        availableSubjects
    }
}

export default SubjectsRequest;